# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Config
# pydantic 2.11.7+ is required by supabase's `realtime` dependency; 2.10.3
# made the tree unresolvable. fastapi/qdrant/openai all accept <3.0.0.
pydantic-settings==2.6.1
pydantic[email]==2.11.7

# Supabase
# Pinned exactly: an unbounded `>=2.30.0` combined with the httpx<0.29 pin below
# makes pip's resolver explore every supabase release and fail with
# `resolution-too-deep`, which breaks the Railway/Nixpacks build too.
supabase==2.31.0

# Vector DB
qdrant-client==1.12.1

# Redis
redis==5.2.1

# LLM + Embeddings
openai==1.57.4

# HTTP client
httpx>=0.26,<0.29

# WebSocket client — Deepgram streaming STT (app/voice/stt_stream.py).
# Previously only present transitively via uvicorn[standard]; pinned here
# because we import it directly and rely on the >=14 `additional_headers` API.
websockets>=14,<16

# Text processing
tiktoken==0.8.0
python-pptx==1.0.2

# Language detection + translation
langdetect==1.0.9
deep-translator==1.11.4

# Utilities
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
aiofiles==24.1.0
