# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12

# Config
# pydantic 2.11.7+ is required by supabase's `realtime` dependency; 2.10.3
# made the tree unresolvable. fastapi/qdrant/openai all accept <3.0.0.
pydantic-settings==2.6.1
pydantic[email]==2.11.7

# Supabase
# Pinned exactly: an unbounded `>=2.30.0` combined with the httpx<0.29 pin below
# makes pip's resolver explore every supabase release and fail with
# `resolution-too-deep`, which breaks the Railway/Nixpacks build too.
supabase==2.31.0

# Vector DB
qdrant-client==1.12.1

# Redis
redis==5.2.1

# LLM + Embeddings
openai==1.57.4

# HTTP client
httpx>=0.26,<0.29

# WebSocket client — Deepgram streaming STT (app/voice/stt_stream.py).
# Previously only present transitively via uvicorn[standard]; pinned here
# because we import it directly and rely on the >=14 `additional_headers` API.
websockets>=14,<16

# Text processing
tiktoken==0.8.0
python-pptx==1.0.2

# Language detection + translation
langdetect==1.0.9
deep-translator==1.11.4

# Utilities
python-jose[cryptography]==3.3.0
# Imported directly by app/shared/crypto.py (Fernet), which keys CRM token
# encryption. It arrives transitively via python-jose today; a transitive
# dependency is not a promise, and this one guards customer credentials.
cryptography>=42,<46
passlib[bcrypt]==1.7.4
aiofiles==24.1.0

# Telephony — Twilio TwiML generation, webhook signature validation, REST client.
# app/voice/telephony.py imports this in four places. The imports are
# function-local so the app still boots without it, which is exactly why its
# absence went unnoticed: the failure only surfaces on the first real call.
twilio==9.11.0

# Document ingestion. These were imported by app/rag/extractor.py but never
# declared, so PDF, DOCX, XLSX and HTML uploads failed in production on an
# ImportError the dashboard surfaced as a raw job error. The imports are
# function-local, which is why the app booted fine and nothing caught it.
#
# OCR (pytesseract, pdf2image) and `unstructured` are deliberately NOT here:
# they need system binaries or half a gigabyte of dependency, and their code
# paths now raise a message a person can act on instead of an ImportError.
pdfplumber==0.11.4
python-docx==1.1.2
openpyxl==3.1.5
beautifulsoup4==4.12.3

# Google Calendar — appointment booking. Same story: imported, never declared,
# so every booking attempt raised ImportError.
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-api-python-client==2.151.0

# Imported directly by app/main.py and app/shared/language.py rather than only
# reached through FastAPI, so it is a real dependency and not a transitive one.
starlette>=0.41,<0.42
